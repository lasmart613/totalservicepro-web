/**
 * /plans tile lines shared by the logged-out and signed-in views.
 * Keep both rows identical for history + AI + manuals.
 */

export const SHARED_SERVICE_HISTORY_LINE = 'Shared service history';

/** Per team member of the organization — not a shared org bucket. */
export const FREE_AI_LINE = '5 text queries/day and 5 voice queries/day per team member';
export const PREMIUM_AI_LINE = '50 text queries/day and 50 voice queries/day per team member';
export const TEAM_AI_LINE = '250 text queries/day and 250 voice queries/day per team member';

export const PREMIUM_MANUALS_LINE = '15 service manuals';
export const TEAM_MANUALS_LINE = 'Unlimited service manuals';
