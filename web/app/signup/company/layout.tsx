import { publicPageMetadata } from '@/lib/seo';

export const metadata = publicPageMetadata('signupCompany');

export default function SignupCompanyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
