import { publicPageMetadata } from '@/lib/seo';

export const metadata = publicPageMetadata('signupOwner');

export default function SignupOwnerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
