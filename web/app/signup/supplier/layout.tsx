import { publicPageMetadata } from '@/lib/seo';

export const metadata = publicPageMetadata('signupSupplier');

export default function SignupSupplierLayout({ children }: { children: React.ReactNode }) {
  return children;
}
