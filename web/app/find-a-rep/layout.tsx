import { publicPageMetadata } from '@/lib/seo';

export const metadata = publicPageMetadata('findARep');

export default function FindARepLayout({ children }: { children: React.ReactNode }) {
  return children;
}
