import { publicPageMetadata } from '@/lib/seo';

export const metadata = publicPageMetadata('marketplaceConsumables');

export default function MarketplaceConsumablesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
